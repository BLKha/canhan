#include <stdio.h>
#include <conio.h>
#include <stdlib.h>

int main(){
	
	
	i = 1;
    count = 0;
    while(i <= n)
    {
        if(n % i == 0)
        {
            if(i % 2 == 0)
            {
            printf("%4d", i);
            count++;
            }
        }
            i++;
    }
    printf("\nSo luong uoc so chan cua %d la: %ld", n, count);

    getch();
    return 0;
}
	getch();
	return 0;
}
